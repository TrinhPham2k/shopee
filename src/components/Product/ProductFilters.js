import React from 'react';
import ProductList from './ProductList';

function ProductFilters() {
    const handleCategoryChange =(newCategoryId)=>{
        // console.log(123);
    }
    return (
        <div>
            {/* <ProductList onChange={handleCategoryChange}/> */}
        </div>
    );
}

export default ProductFilters;